#include "holberton.h"
/**
* _isupper - description for this function
* @c: parameter
* Return: Always 0 (Success)
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
_putchar ('1');
else
_putchar ('\0');
return(0);
}
