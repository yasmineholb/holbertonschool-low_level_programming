#include "holberton.h"
#include<stdio.h>
#include<stdlib.h>
/**
 * _abs - description for this function
 *@n: parameter
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
int s;
s = abs(n);
return (s);
}
