#include <stdio.h>
#include <stdlib.h>
#include "holberton.h"
/**
 *
 *
 *
 */
int main(int argc, char **argv)
{
int res = 0;
int i;
for (i = 1; i < argc; i++)
{
if (!atoi(argv[i]))
{
printf("Error\n");
return (1);
}
res += atoi(argv[i]);
}
printf("%d\n", res);
return (0);
}
