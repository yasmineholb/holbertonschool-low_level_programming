#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
size_t list_len(const list_t *h)
{
size_t k;
while (h != NULL)
{
k++;
h = h->next;
}
return (k);
}
